package event;

public class SamePos extends OldCar {
	
	public SamePos (double type, double sec, double min, double vid, double spd, double xway, double lane, double dir, double seg, double pos) {
		super(type, sec, min, vid, spd, xway, lane, dir, seg, pos);
	}
}
